Spark operations on distributed data can be classified into two types transformations and actions Transformations as the name suggests transform a Spark DataFrame
into a new DataFrame without altering the original dat giving it the property of immutability Put another way an operation such as select or filter will not
change the original DataFrame instead it will return the transformed results of the operation as a new DataFrame